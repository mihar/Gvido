%h4
  - if link === true
    = link_to location.title, location_path(location)
  - else
    = location.title
= spit location, :subtitle
.address
  = location.address
  %br
  = location.zip
  = location.city