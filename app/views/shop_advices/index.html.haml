- title "Nakupovalni nasveti za #{parent.title}"


.instructions
  = (textilize parent.shop_instructions_html).html_safe
.instrument_image= (image_tag @instrument.icon.url(:normal)).html_safe


= render @shop_advices

