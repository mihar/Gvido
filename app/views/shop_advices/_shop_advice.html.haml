.advice
  - if shop_advice.photo.file?
    .image= (link_to_lightbox image_tag(shop_advice.photo.url(:square)), shop_advice.photo.url(:normal), shop_advice.id, "").html_safe
  - if shop_advice.description
    .body= (textilize shop_advice.description_html).html_safe
  - if shop_advice.url
    .link= link_to "Povezava", shop_advice.url
  
  .clear
