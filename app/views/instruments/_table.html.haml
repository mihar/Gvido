%table
  %tr
    - instruments.each do |i|    
      %td= link_to image_tag(i.icon_medium, :title => i.title), instrument_path(i)
  %tr
    - instruments.each do |i|
      %td= link_to i.title, instrument_path(i)