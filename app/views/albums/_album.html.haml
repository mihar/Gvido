.album
  %h3= link_to album.title, album
  - unless album.photos.empty?
    = link_to image_tag(album.photos.first.photo.url(:mini)), album_path(album)
    
  - admin_section do
    = link_to "Uredi", edit_album_path(album)
    |
    = link_to "Odstrani", album, :confirm => 'Ste prepričani?', :method => :delete