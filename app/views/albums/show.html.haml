- title @album.title
- @lightbox = true

- unless @album.description.blank?
  = render_misc 'albums'

%ul.photos
  = render :partial => @album.photos
  %li.clear

%p.goback
  =link_to "Nazaj", albums_path
