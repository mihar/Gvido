%h2 Prijava v sistem
= simple_form_for resource, :as => resource_name, :url => user_session_path do |f|
  = f.input :email
  = f.input :password
  = f.input :remember_me, :as => :boolean
  
  .submit= f.submit "Prijava"
